<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorBlack</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Transparent" parent="@android:style/Theme.Material.Light.Dialog.NoActionBar">
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

<!--    <style name="RatingBarProduct" parent="Widget.AppCompat.RatingBar.Small">-->
<!--        <item name="android:minHeight">15dp</item>-->
<!--        <item name="android:minWidth">15dp</item>-->
<!--        <item name="colorControlNormal">@color/colorGray</item>-->
<!--        <item name="colorControlActivated">@color/colorGold</item>-->
<!--    </style>-->

    <style name="MyRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:minHeight">10dp</item>
        <item name="android:maxHeight">10dp</item>
        <item name="colorControlNormal">@color/colorGray</item>
        <item name="colorControlActivated">@color/colorGold</item>
    </style>

</resources>